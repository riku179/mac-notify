{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/mac_parse",
            "args": ['enp2s0f0'],
            "cwd": "${workspaceRoot}"
        }
    ]
}
